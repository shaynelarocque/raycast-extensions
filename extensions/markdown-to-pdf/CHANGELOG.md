# Changelog

## [Initial Release] - {PR_MERGE_DATE}

- AI tool for converting markdown to PDF
- Custom PDF renderer with styled text, lists, tables, code blocks, and hyperlinks
- Optional layout controls: font, font size, line spacing, and per-side margins
- Built-in color themes: default, minimal, executive, ocean, warm
- Custom theme support via extension preferences
